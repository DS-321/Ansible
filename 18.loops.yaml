- name: install packages
  hosts: frontend
  become: yes
  tasks:
  - name: install packages
    ansible.install.package:
      name: "{{ item }}"
      state: installed
    loop:
    - mysql
    - nginx
    - zip